export enum ResourceType {
    FOOD = 'food',
    WOOD = 'wood',
    STONE = 'stone',
    GOLD = 'gold',
};

const gameConfig = {
    STARTING_ENERGY_MAX: 3,
    STARTING_XP: 0,
};

export default gameConfig;